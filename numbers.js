//integers number

let counter=100;
console.log(counter);

// Integers can be represented in the following formats:

// Octal (base 8):start with 0
// Hexadecimal (based 16): 0x 0X

//octal numbers
let num=071;
console.log(num);

let nu=080;
console.log(nu);

//hexadecimal
let nm=0x1a;
console.log(nm);

